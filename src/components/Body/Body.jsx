import Header from "../Header/Header";
import ItemListContainer from "../ItemListContainer/ItemListContainer";

const Body = () => {
    return(
        <section>
            <Header/>
            <ItemListContainer/>
        </section>
    );
};

export default Body;